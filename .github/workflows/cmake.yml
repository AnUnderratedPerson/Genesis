name: CMake

on:
  push:
    branches: [ "main", "pre-release" ]
  pull_request:
    branches: [ "main", "pre-release" ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Debug # Eventually release
  BUILD_ENV: ${{github.workspace}}/build

jobs:
  build:
    # The CMake configure and build commands are platform agnostic and should work equally well on Windows or Mac.
    # You can convert this to a matrix build if you need cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: cmake -B ${{BUILD_ENV}} -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build
      # Build your program with the given configuration
      run: cmake --build ${{BUILD_ENV}} --config ${{env.BUILD_TYPE}}
      
      # Changing the current directory for easy testing
    - working-directory: ${{BUILD_ENV}}

    - name: Run Appplicatuion with Input/s
      run: |
          mkdir internal_test
          cp ../test/* internal_test
          ls internal_test
          ./Genesis test/syntax.txt

    - name: CTest
      # Execute tests defined by the CMake configuration.  
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest -C ${{env.BUILD_TYPE}}
          
    - name: Deinitialization Cleanup
      run: |
          rm test/*
          rmdir test
